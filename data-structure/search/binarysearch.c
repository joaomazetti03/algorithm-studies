#include <stdio.h>

void binarysearch();

int main(){
    printf("Hello World!");

    return 0;
}

void binarysearch(list, iten){
    int left = 0;
    int right = sizeof(list) - 1;

    while(left <= right){
        int mid = (left + right) % 2;
        
    }
}