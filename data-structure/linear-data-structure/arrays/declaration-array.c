/*
declaration types of arrays

Arrays are declarated in 3 type forms, one-dimensional, multi-dimensional(matrix) and three-dimensional

1D: int arr[];
2D: int arr[][];
3D: int arr[][][];

Is possible to inicialized arrys with repetition structure, like FOR.
*/

# include <stdio.h>

int main(){
    //inicialize methods 
    int a[3] = {1, 2, 3};
    
    int b[] = {1, 2, 3, 4};
    
    int c[1];
    c[1] = 2;

    return 0;
}